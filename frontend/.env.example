# Copy to `.env` (or `.env.local`) to override defaults. Vite only exposes
# variables prefixed with VITE_ to the app.

# Where the Cadence backend is running. Defaults to http://127.0.0.1:8010.
# Change this if you run the backend on a different port.
# VITE_API_BASE=http://127.0.0.1:8010
